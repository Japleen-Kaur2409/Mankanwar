/* Hero Section */
.hero {
    text-align: center;
    padding: 2rem 0;
}

.hero img {
    max-width: 150px;
    margin-bottom: 20px;
}

.hero h1 {
    font-size: 2.5rem;
}

.hero p {
    font-size: 1.2rem;
}

button {
    background-color: var(--ubc-gold);
    color: white;
    padding: 0.5rem 1rem;
    border: none;
    cursor: pointer;
    font-size: 1rem;
    margin-top: 20px;
}

button:hover {
    background-color: #e2a100;
}
